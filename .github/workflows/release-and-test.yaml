name: Release Charts

on:
  pull_request:
    branches:
      - '*'
  push:
    branches:
      - "*"
env:
  HELM_VERSION: v3.4.0

jobs:
  find-charts:
    name: Find Charts
    runs-on: ubuntu-latest
    outputs:
      matrix: "${{ steps.get_changed_charts_folders.outputs.matrix }}"
    steps:
      - uses: actions/checkout@v1
      - name: Get changed charts
        id: get_changed_charts
        uses: tj-actions/changed-files@v35.9.1
        with:
            json: true
            path: "./charts/"
            diff_relative: true
            dir_names_max_depth: 1
            dir_names: true
            dir_names_exclude_root: true
            write_output_files: true
      - name: Echo changed files
        id: get_changed_charts_folders
        run: echo matrix=${{ steps.get_changed_charts.outputs.all_changed_files }} >> $GITHUB_OUTPUT
  release-charts:
    name: Check
    needs: find-charts
    if: ${{ needs.find-charts.outputs.matrix != '' && toJson(fromJson(needs.find-charts.outputs.matrix)) != '[]' }}
    strategy:
      matrix:
        chart: ${{fromJSON(needs.find-charts.outputs.matrix)}}
    permissions:
      contents: write
      security-events: write
      actions: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Configure Git
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"

      - name: Setup Helm
        uses: azure/setup-helm@v3
        with:
          version: ${{ env.HELM_VERSION }}
#
#      - name: Trivy Scan ${{ matrix.chart }}
#        uses: aquasecurity/trivy-action@0.10.0
#        with:
#          scan-type: 'config'
#          hide-progress: false
#          format: 'sarif'
#          scan-ref: 'charts/${{matrix.chart}}'
#          output: 'trivy-results.sarif'
#          exit-code: '1'
#          ignore-unfixed: false
#          severity: 'MEDIUM,HIGH,CRITICAL'
#          limit-severities-for-sarif: true
#
#      - name: Lint ${{ matrix.chart }}
#        run: |
#          helm dependency update charts/${{ matrix.chart }}
#          helm lint charts/${{ matrix.chart }} --strict
#
#      - name: KubeVal ${{ matrix.chart }}
#        run: |
#          helm plugin install https://github.com/instrumenta/helm-kubeval
#          helm kubeval --ignore-missing-schemas charts/${{ matrix.chart }}
#
#      - name: Upload Trivy scan results for ${{ matrix.chart }}
#        uses: github/codeql-action/upload-sarif@v2
#        if: always()
#        with:
#          sarif_file: 'trivy-results.sarif'

      - name: Move to publish folder
        run: |
         mkdir publish-charts
         mv charts/${{ matrix.chart }} publish-charts/${{ matrix.chart }}

      - name: Publish ${{ matrix.chart }}
        uses: helm/chart-releaser-action@v1.5.0
        env:
          charts_dir: publish-charts
          CR_TOKEN: "${{ secrets.GITHUB_TOKEN }}"

      - name: Helm Chart Docs Github Pages
        uses: omegion/helm-charts-docs@v0.15
        with:
          helmRepository: iits-charts
          helmRepositoryUrl: https://charts.iits.tech
          token: ${{ secrets.GITHUB_TOKEN }}