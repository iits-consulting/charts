{{- if .Values.middleware.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ollama-middleware.fullname" . }}
  labels:
    {{- include "ollama-middleware.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ollama-middleware.fullname" . }}
  minReplicas: {{ .Values.middleware.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.middleware.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.middleware.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.middleware.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.middleware.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.middleware.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
